export const role =
  "user" || "admin" || "cashier" || "manager" || "CEO" || "account_holder";

export type TUserRole = 
|"user"
| "admin"
| "cashier"
| "manager"
| "CEO"
| "account_holder";

export enum ENUM_USER_ROLES {
  USER = "user",
  ADMIN = "admin",
  CASHIER = "cashier",
  MANAGER = "manager",
  CEO = "CEO",
  ACCOUNT_HOLDER = "account_holder",
}
