/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "i.imgur.com", "images.unsplash.com", "avatars0.githubusercontent.com", "vojislavd.com", "raw.githubusercontent.com"],
  },
};

module.exports = nextConfig
